{
  "name": "sample",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive test",
    "coverage": "c8 --all --report-dir .coverage/ --reporter cobertura --reporter text mocha --recursive test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "c8": "7.12.0",
    "mocha": "10.2.0"
  }
}
